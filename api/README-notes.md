# 🧮 Commission Calculator — Technical Assessment

It connects a React (Vite) frontend with a .NET backend API that performs commission calculations for local and foreign sales.

Frontend Framework – React (Vite)

Used fetch API for simplicity instead of Axios to avoid extra dependencies.

Backend – .NET 6 Web API.

CORS enabled for local frontend development.
